<script type='text/javascript'>
  google.maps.event.addDomListener(window, 'load', initMap);

</script>
<style>
  #map{
    height:100%;
    margin:0;
}
</style>
<div class="fullscreen">
  <nav>
    <ul class="list-unstyled main-menu">
      <li class='text-right'>
        <span> Signed as <%= current_user.email %>&nbsp;&nbsp;</span>
        <a href="#" id="nav-close" class="text-right">X</a>
      </li>
      <li class='text-right'><%= link_to 'Log out',destroy_user_session_path, :method => :delete %></li>
      <% @category.each do |c| %>
        <li><a href="#">
          <label><input type="checkbox"  id='<%= c %>' onclick="checkbox('<%= c %>')" > <%= c %></label>
        </a></li>
      <% end %>
      <li><a><label><input type="checkbox" id='circle' onclick="checkCircle('circle')" > Within 10 min</label></a></li>
    </form>
    </ul>
  </nav>

  <a id="nav-expander" class="nav-expander fixed" style="">
    <i class="fa fa-bars fa-lg white" ></i>
  </a>
</div>

<div id="map"></div>
